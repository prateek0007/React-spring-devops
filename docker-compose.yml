services:

  backend:
    build:
      context: ./backend
    container_name: spring-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: react-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
